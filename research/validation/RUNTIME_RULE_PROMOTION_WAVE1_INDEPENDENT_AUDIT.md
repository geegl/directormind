# Runtime Rule Promotion Wave 1 — Independent Audit

Updated: 2026-09-03

Status: `INITIAL_REVIEW_FAIL / TWO_P1_REPAIRED_LOCAL / RE_REVIEW_PENDING`

## Review independence and scope

A non-writing reviewer used a clean detached checkout, independently located
and reopened all nine unique local evidence units, replayed the three promotion
chains and six target forward packages, ran the complete repository contract,
and checked PR #4 and its hosted validation. The review did not write rules,
repository files or remote state.

## Initial must-fix findings

1. B99 S016 retained a visible foreground counterpart and was an
   owner-dominant OTS, while the canonical Wave 1 review incorrectly called it
   a clean single and counted it as the same clean-single mechanism.
2. Two original boundary inputs asserted mutually exclusive routing signals.
   The proximity positive and boundary packages also lacked an explicit locked
   story fact authorizing their `ROMANTIC_PROXIMITY` classification.

The initial verdict was `FAIL` with two P1 findings and no P0.

## Local repair and evidence

- B99 remains a freshly viewed adjacent example but no longer contributes a
  support relation. Its observation now records the foreground shoulder and
  OTS composition.
- The performance rule retains two unrelated real works: Mr. Robot as the
  source and Better Call Saul as support. Better Call Saul S082 now establishes
  the pre-hold relation before S097–S098.
- The spatial boundary keeps only `counterpart_relation_not_required`; the
  proximity ellipsis boundary keeps only `elliptical_time_change` and no longer
  claims continuous present time.
- Both project-original proximity packages now explicitly lock their romantic
  relationship context. Continuous-time and ellipsis claims each bind to their
  own locked fact type.
- Forward validation now rejects the two observed contradictory signal pairs,
  active runtime signals without the required locked fact type, and a romantic
  scene-problem label without relationship authority.
- The repaired packages still yield three correct positive selections and
  three target-rule boundary rejections. All remain `HUMAN_REVIEW_PENDING` and
  unauthorized for generation or publication.
- Local verification passes 244 tests and all 21 repository checks.

## Pending exit condition

This record does not self-approve the repair. The status may advance only after
the repaired commit is pushed, hosted CI succeeds on that head, and the
independent reviewer replays both P1 cases from a new clean checkout and returns
no must-fix finding.

## Preserved boundaries

Semantic audio, exact lens/support metadata, some cross-cut identity and
performance causality remain unverified. No source media, legacy evidence
ledger, private script, production system, account, permission, payment,
deployment or publication surface was changed.
